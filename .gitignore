node_modules
.next
# next-pwa outputs (created by `npm run build`; Vercel regenerates on deploy)
public/sw.js
public/precache.*.js
.env.local
# .env.local.example is intentionally committed — it contains only placeholder values
.env.local.example.example
